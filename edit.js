const button1 = document.getElementById("btn");

//edit----------------------------------------------------------------------------------------------------------------------------------

function redirect(id) {
  window.location.replace(`http://127.0.0.1:5500/Home.html?id=${id}`);
}
